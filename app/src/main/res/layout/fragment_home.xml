<?xml version="1.0" encoding="utf-8"?>
<su.leff.androidtemplate.view.ParentConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:fitsSystemWindows="true">

    <FrameLayout
        android:id="@+id/root"
        android:background="#ff0fff"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/bottomBar"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <me.ibrahimsn.lib.SmoothBottomBar
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        app:backgroundColor="@color/background_accent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/menu_bottom" />
</su.leff.androidtemplate.view.ParentConstraintLayout>
